import React from 'react';

const PageNotFound = () => {
    return (
        <div>
           <p style={{color:'blue',textDecoration:'underline'}}>Page not Found </p>
           <img src='/cookie-imgs/missing_piece_cookie.jpg' alt="pretty_header"/>
           <p style={{color:'blue',textDecoration:'underline'}}>Missing cookie piece cannot be found Here either</p>
        </div>
    );
};

export default PageNotFound